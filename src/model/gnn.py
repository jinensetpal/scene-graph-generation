#!/usr/bin/env python3

import torch.nn as nn
import torch

class Node:
    def __init__(self, value):
        self.value = value

class Graph:
    def __init__(self):
        pass

class GNN(nn.Module):
    def __init__(self):
        pass
        #self.graph = graph
        #self.adj = torch.zeros(graph.shape)
        #self.adj.fill_diagonal_(1)
